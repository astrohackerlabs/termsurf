# Render Channel

Shared C ABI for Astrohacker Terminal render side-channel experiments.

The files are named `termsurf_render_channel.*` because the ABI belongs to the
TermSurf protocol layer used by Astrohacker Terminal engines. Keep the name
stable unless an issue explicitly changes the protocol ABI.
