From 40cbdae53f84338831428ac0e1860850655e7fea Mon Sep 17 00:00:00 2001
From: astrohacker <dev@local>
Date: Sun, 16 Aug 2026 11:42:49 -0500
Subject: [PATCH 31/37] Chrome-parity Mac wheel and scroll routing

---
 content/libtermsurf_chromium/BUILD.gn         |  2 +
 .../libtermsurf_chromium.h                    |  5 +-
 .../ts_browser_main_parts.cc                  | 60 ++++++++++++++-----
 .../libtermsurf_chromium/ts_wheel_route_mac.h | 27 +++++++++
 .../ts_wheel_route_mac.mm                     | 18 ++++++
 5 files changed, 96 insertions(+), 16 deletions(-)
 create mode 100644 content/libtermsurf_chromium/ts_wheel_route_mac.h
 create mode 100644 content/libtermsurf_chromium/ts_wheel_route_mac.mm

diff --git a/content/libtermsurf_chromium/BUILD.gn b/content/libtermsurf_chromium/BUILD.gn
index 8b9c47d736..85c114b8bd 100644
--- a/content/libtermsurf_chromium/BUILD.gn
+++ b/content/libtermsurf_chromium/BUILD.gn
@@ -97,6 +97,8 @@ shared_library("libtermsurf_chromium") {
       "ts_compositor_bridge_mac.mm",
       "ts_shell_window_mac.h",
       "ts_shell_window_mac.mm",
+      "ts_wheel_route_mac.h",
+      "ts_wheel_route_mac.mm",
     ]
   }
 
diff --git a/content/libtermsurf_chromium/libtermsurf_chromium.h b/content/libtermsurf_chromium/libtermsurf_chromium.h
index a037fa5015..4c58266d6d 100644
--- a/content/libtermsurf_chromium/libtermsurf_chromium.h
+++ b/content/libtermsurf_chromium/libtermsurf_chromium.h
@@ -114,7 +114,10 @@ TS_EXPORT void ts_forward_mouse_move(ts_web_contents_t wc,
                                      int y,
                                      int modifiers);
 
-/* phase/momentum_phase: 0=none, 1=began, 2=changed, 3=ended.
+/* phase / momentum_phase: AppKit NSEventPhase bitmasks (same layout as Blink
+   WebMouseWheelEvent::Phase): 0=none, began=1<<0, stationary=1<<1,
+   changed=1<<2, ended=1<<3, cancelled=1<<4, mayBegin=1<<5.
+   Host (TermSurf) forwards NSEvent.phase / momentumPhase raw values.
    Synchronous — safe. */
 TS_EXPORT void ts_forward_scroll_event(ts_web_contents_t wc,
                                        int x,
diff --git a/content/libtermsurf_chromium/ts_browser_main_parts.cc b/content/libtermsurf_chromium/ts_browser_main_parts.cc
index 059ce1d7e6..b85832c49f 100644
--- a/content/libtermsurf_chromium/ts_browser_main_parts.cc
+++ b/content/libtermsurf_chromium/ts_browser_main_parts.cc
@@ -64,12 +64,15 @@
 #include "content/libtermsurf_chromium/ts_ca_layer_bridge_mac.h"
 #include "content/libtermsurf_chromium/ts_compositor_bridge_mac.h"
 #include "content/libtermsurf_chromium/ts_shell_window_mac.h"
+#include "content/libtermsurf_chromium/ts_wheel_route_mac.h"
 #include "content/public/browser/context_factory.h"
 #include "third_party/blink/public/common/input/web_mouse_event.h"
 #include "third_party/blink/public/common/input/web_mouse_wheel_event.h"
+#include "third_party/blink/public/common/input/web_pointer_properties.h"
 #include "third_party/blink/public/mojom/frame/find_in_page.mojom.h"
 #include "third_party/blink/public/mojom/frame/user_activation_notification_type.mojom.h"
 #include "third_party/blink/public/mojom/input/input_handler.mojom.h"
+#include "ui/events/event.h"
 #include "ui/events/keycodes/dom/dom_code.h"
 #include "ui/events/keycodes/dom/dom_key.h"
 #include "ui/events/keycodes/keyboard_code_conversion.h"
@@ -968,6 +971,20 @@ void TsBrowserMainParts::ForwardScrollEvent(void* wc_handle,
     return;
   }
 
+  // TermSurf host (AppKit) sends NSEvent.phase / momentumPhase raw bitmasks.
+  // blink::WebMouseWheelEvent::Phase uses the same bit layout as NSEventPhase:
+  //   none=0, began=1<<0, stationary=1<<1, changed=1<<2, ended=1<<3,
+  //   cancelled=1<<4, mayBegin=1<<5, blocked=1<<6 (Blink-only).
+  // Do not remap to a linear 0..3 enum — that breaks momentum/fling.
+  constexpr int kBlinkWheelPhaseMask =
+      blink::WebMouseWheelEvent::kPhaseBegan |
+      blink::WebMouseWheelEvent::kPhaseStationary |
+      blink::WebMouseWheelEvent::kPhaseChanged |
+      blink::WebMouseWheelEvent::kPhaseEnded |
+      blink::WebMouseWheelEvent::kPhaseCancelled |
+      blink::WebMouseWheelEvent::kPhaseMayBegin |
+      blink::WebMouseWheelEvent::kPhaseBlocked;
+
   int web_modifiers = modifiers & 0xF;
 
   blink::WebMouseWheelEvent wheel_event(blink::WebInputEvent::Type::kMouseWheel,
@@ -975,14 +992,25 @@ void TsBrowserMainParts::ForwardScrollEvent(void* wc_handle,
 
   wheel_event.SetPositionInWidget(gfx::PointF(x, y));
   wheel_event.SetPositionInScreen(gfx::PointF(x, y));
+  wheel_event.button = blink::WebPointerProperties::Button::kNoButton;
   wheel_event.delta_x = delta_x;
   wheel_event.delta_y = delta_y;
-  wheel_event.phase = static_cast<blink::WebMouseWheelEvent::Phase>(phase);
-  wheel_event.momentum_phase =
-      static_cast<blink::WebMouseWheelEvent::Phase>(momentum_phase);
+  // Match MakeWebMouseWheelEventFromUiEvent: ticks from pixel/line delta.
+  wheel_event.wheel_ticks_x =
+      delta_x / static_cast<float>(ui::MouseWheelEvent::kWheelDelta);
+  wheel_event.wheel_ticks_y =
+      delta_y / static_cast<float>(ui::MouseWheelEvent::kWheelDelta);
+  wheel_event.phase = static_cast<blink::WebMouseWheelEvent::Phase>(
+      phase & kBlinkWheelPhaseMask);
+  wheel_event.momentum_phase = static_cast<blink::WebMouseWheelEvent::Phase>(
+      momentum_phase & kBlinkWheelPhaseMask);
   wheel_event.delta_units = precise
                                 ? ui::ScrollGranularity::kScrollByPrecisePixel
                                 : ui::ScrollGranularity::kScrollByLine;
+  // Default constructor leaves event_action = kPageZoom; set scroll intent.
+  wheel_event.event_action =
+      blink::WebMouseWheelEvent::GetPlatformSpecificDefaultEventAction(
+          wheel_event);
 
   auto* web_contents = tab->shell->web_contents();
   auto* view = web_contents->GetRenderWidgetHostView();
@@ -999,25 +1027,27 @@ void TsBrowserMainParts::ForwardScrollEvent(void* wc_handle,
               << " router=" << router << " coords=(" << x << ", " << y
               << ") delta=(" << delta_x << ", " << delta_y
               << ") phase=" << phase << " momentum_phase=" << momentum_phase
+              << " ticks=(" << wheel_event.wheel_ticks_x << ", "
+              << wheel_event.wheel_ticks_y << ")"
+              << " event_action="
+              << static_cast<int>(wheel_event.event_action)
               << " precise=" << (precise ? 1 : 0) << " modifiers=" << modifiers;
 
     LogIssue794InputFrames("[termsurf-pdf-input]", web_contents, x, y);
   }
 
-  if (view && router) {
-    auto* root_view = static_cast<RenderWidgetHostViewBase*>(view);
-    if (Issue794InputTraceEnabled()) {
-      LOG(INFO) << "[termsurf-pdf-input] wheel-route route_mode=input-router"
-                << " root_view=" << root_view << " router=" << router;
-    }
-    ui::LatencyInfo latency;
-    router->RouteMouseWheelEvent(root_view, &wheel_event, latency);
-  } else if (view) {
+  // Issue 26072214390772 Exp 3: Prefer Mac RWHV phase-handler path so physical
+  // phase=ended is delayed (~100ms) until momentum begins — same as Chrome's
+  // RenderWidgetHostViewMac wheel entry. Bare RouteMouseWheelEvent bypasses
+  // mouse_wheel_phase_handler_ and can kill coast at the phase boundary.
+  // RouteWheelEventMac lives in .mm (cannot include RWHV-Mac headers from .cc).
+  if (view) {
     if (Issue794InputTraceEnabled()) {
-      LOG(INFO) << "[termsurf-pdf-input] wheel-route route_mode=direct-fallback"
-                << " reason=" << (router ? "missing-view" : "missing-router");
+      LOG(INFO) << "[termsurf-pdf-input] wheel-route "
+                << "route_mode=mac-route-or-process"
+                << " view=" << view << " router=" << router;
     }
-    view->GetRenderWidgetHost()->ForwardWheelEvent(wheel_event);
+    RouteWheelEventMac(view, wheel_event);
   } else if (Issue794InputTraceEnabled()) {
     LOG(INFO) << "[termsurf-pdf-input] wheel-route route_mode=drop"
               << " reason=missing-view";
diff --git a/content/libtermsurf_chromium/ts_wheel_route_mac.h b/content/libtermsurf_chromium/ts_wheel_route_mac.h
new file mode 100644
index 0000000000..a843159a65
--- /dev/null
+++ b/content/libtermsurf_chromium/ts_wheel_route_mac.h
@@ -0,0 +1,27 @@
+// Copyright 2026 TermSurf
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#ifndef CONTENT_LIBTERMSURF_CHROMIUM_TS_WHEEL_ROUTE_MAC_H_
+#define CONTENT_LIBTERMSURF_CHROMIUM_TS_WHEEL_ROUTE_MAC_H_
+
+// C++ header: routes a WebMouseWheelEvent through Mac RWHV phase-handler path
+// (RouteOrProcessWheelEvent). Implementation is ObjC++ (.mm) so pure .cc files
+// do not include RenderWidgetHostViewMac.h / Cocoa.
+
+namespace blink {
+class WebMouseWheelEvent;
+}
+
+namespace content {
+
+class RenderWidgetHostView;
+
+// Prefer Chrome Mac delayed phase-end / momentum path. |view| must be the
+// product Mac RWHV (same cast pattern as compositor bridge).
+void RouteWheelEventMac(RenderWidgetHostView* view,
+                        const blink::WebMouseWheelEvent& wheel_event);
+
+}  // namespace content
+
+#endif  // CONTENT_LIBTERMSURF_CHROMIUM_TS_WHEEL_ROUTE_MAC_H_
diff --git a/content/libtermsurf_chromium/ts_wheel_route_mac.mm b/content/libtermsurf_chromium/ts_wheel_route_mac.mm
new file mode 100644
index 0000000000..89aea6482f
--- /dev/null
+++ b/content/libtermsurf_chromium/ts_wheel_route_mac.mm
@@ -0,0 +1,18 @@
+// Copyright 2026 TermSurf
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#import "content/libtermsurf_chromium/ts_wheel_route_mac.h"
+
+#include "content/browser/renderer_host/render_widget_host_view_mac.h"
+#include "third_party/blink/public/common/input/web_mouse_wheel_event.h"
+
+namespace content {
+
+void RouteWheelEventMac(RenderWidgetHostView* view,
+                        const blink::WebMouseWheelEvent& wheel_event) {
+  auto* mac_view = static_cast<RenderWidgetHostViewMac*>(view);
+  mac_view->RouteOrProcessWheelEvent(wheel_event);
+}
+
+}  // namespace content
-- 
2.50.1 (Apple Git-155)

